'use server';

export default async function getHostSubscriptions() {
  // const session = await getSession();
  // if (!session) {
  //     throw new Error('no session found')
  // }
  // const {session, user} = session
  // const asdf = await db.query.hostSubscription.findMany({where: {
  //     eq:
  // }})
}
